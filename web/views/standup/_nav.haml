- previous_standup = Standup.previous(team.name, standup_date)
- next_standup = Standup.next(team.name, standup_date)
- today = Date.today.to_s

%p
  %h4
    - if previous_standup
      %a{href: "/team/#{team.name}/standups/#{previous_standup.created_at.to_date}"}<
        previous
    - else
      previous
    |
    - if next_standup
      %a{href: "/team/#{team.name}/standups/#{next_standup.created_at.to_date}"}<
        next
    - else
      next
    |
    - if standup_date != today
      %a{href: "/team/#{team.name}/standups/#{today}"}<
        today
    - else
      today
